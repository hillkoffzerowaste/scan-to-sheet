# DESIGN.md — Modern Windows Workspace

ระบบดีไซน์ของ scan-to-sheet เป็นแหล่งอ้างอิงเดียวของสี สเกล component และเกณฑ์ตรวจ UI ทุกค่าต้องประกาศเป็น CSS variable ใน `src/styles.css` ไม่ hardcode ที่จุดใช้งาน ปัจจุบัน shell desktop ใช้ขอบเขต `.wireframe-shell` และ token กลุ่ม `--wire-*` เป็นภาษาภาพหลัก

## 1. หลักการและขอบเขต

Modern Windows workspace ใช้กับ Packer/Admin รายงาน และพนักงาน/ตารางงาน โดยเน้นการอ่านข้อมูลและคำสั่งภายในมากกว่าการตกแต่ง แต่คง popup สแกน พฤติกรรม QR และระบบบันทึกเดิม ไม่เปลี่ยน API หรือข้อมูลปฏิบัติงานใน Firestore/Sheet; เอกสารตั้งค่า UI ส่วนกลางอาจเพิ่ม preference ได้เมื่อมีฟีเจอร์รองรับ

- Desktop-only: ตรวจที่ 1280/1440/1920px และให้ช่องสแกนเห็นได้ที่ 1280×720 ไม่มีการเพิ่ม mobile layout
- แถบบนเดียว 56px; เมนูซ้าย 224px ยุบเหลือ 64px; หัวหน้าและคำสั่งอยู่ด้วยกัน; แถบสถานะล่าง 24px
- สีระบบเป็นเทา–น้ำเงินแบบ Windows รุ่นใหม่และมี dark mode; สีสถานะใช้เพื่อสื่อความหมายเท่านั้น
- ทุก panel/control ใช้เส้น 1px ที่เห็นได้ พื้นทึบ และตัวบ่งชี้รายการที่เลือกชัดเจน ใช้มุมโค้งเล็กน้อยแทนกรอบแข็งแบบเดิม แต่ยังไม่ใช้เงาหรือพื้น tint เป็นตัวสื่อสารเพียงอย่างเดียว
- ปุ่มสูงขั้นต่ำ 36px; มุมทั้งหมดใช้ `--wire-radius: 6px`; ระยะห่างหลัก 16px
- ไม่มี hover lift, gradient หรือ glassmorphism; shadow อนุญาตเฉพาะ dialog/dropdown
- `.wireframe-shell` คือขอบเขตหน้าตาปัจจุบัน ส่วนชื่อ class `win-*` และ `enterprise-shell` ยังเก็บไว้เป็น compatibility hooks และ DOM contract
- CSS เก่ายังมีอยู่ ให้แก้กฎต้นทางที่เกี่ยวข้อง ไม่ซ้อน `!important` หรือเพิ่มชุด override ทั้งระบบ
- รูปแบบพิมพ์ตารางเวรและ QR ต้องรักษาโครงกระดาษ ไม่รับขนาด control หรือเปลือกหน้าจอใหม่

### Modern Windows workspace tokens

| token | light | dark | ใช้ที่ |
|---|---|---|---|
| `--wire-window` | `#eef1f4` | `#151b22` | พื้น workspace และ shell |
| `--wire-face` | `#f6f7f9` | `#202831` | toolbar และ control |
| `--wire-paper` | `#ffffff` | `#1c242c` | panel, table, input |
| `--wire-titlebar` | `#222a33` | `#151a20` | topbar เรียบแบบ internal tool |
| `--wire-sidebar` | `#282f38` | `#1b222a` | sidebar หลัก |
| `--wire-sidebar-active` | `#1f6bc5` | `#256bb2` | เมนูที่เปิดอยู่ |
| `--wire-selection` | `#dcecff` | `#3b78b8` | รายการ/ปุ่มที่เลือกใน workspace |
| `--wire-control-line` | `#71808e` | `#9fb0c0` | ขอบ control |
| `--wire-text` | `#202932` | `#edf4fb` | ตัวหนังสือหลัก |
| `--wire-muted` | `#536170` | `#b7c3cf` | label และข้อความรอง |
| `--wire-radius` | `6px` | `6px` | มุมทุก component ใน shell |

ลิงก์ภายนอกใช้สีจัดหมวด 10 สีบนพื้นทั้งแถบ (ไม่สื่อสถานะ) สีพื้นผสม accent 24% กับ `--wire-sidebar`; hover เพิ่มเป็น 32% โดยมีขอบบนสว่าง ขอบล่างลึก และแถบซ้ายสี accent เพื่อให้เกิดมิติแบบทึบโดยไม่ใช้ shadow/gradient ตัวหนังสือบนพื้นแถบปกติได้ contrast อย่างน้อย 6.29:1 และตอน hover อย่างน้อย 5.10:1 ทั้ง light/dark

| token | light / dark | contrast ข้อความบนแถบ light / dark |
|---|---|---|
| `--wire-sidebar-accent-slate` | `#cbd5e1` | 6.56 / 7.61 |
| `--wire-sidebar-accent-blue` | `#93c5fd` | 7.06 / 8.18 |
| `--wire-sidebar-accent-cyan` | `#67e8f9` | 6.60 / 7.54 |
| `--wire-sidebar-accent-teal` | `#5eead4` | 6.62 / 7.66 |
| `--wire-sidebar-accent-lime` | `#bef264` | 6.29 / 7.29 |
| `--wire-sidebar-accent-purple` | `#c4b5fd` | 7.15 / 8.27 |
| `--wire-sidebar-accent-violet` | `#d8b4fe` | 7.03 / 8.16 |
| `--wire-sidebar-accent-pink` | `#fda4af` | 7.28 / 8.46 |
| `--wire-sidebar-accent-orange` | `#fdba74` | 7.02 / 8.14 |
| `--wire-sidebar-accent-amber` | `#fcd34d` | 6.62 / 7.67 |

สัดส่วน tint และ edge: `--wire-sidebar-accent-fill` 24%, `--wire-sidebar-accent-border` 54%, `--wire-sidebar-accent-hover-fill` 32%, `--wire-sidebar-accent-hover-border` 70%, `--wire-sidebar-accent-highlight` 58%, `--wire-sidebar-accent-depth` 44%

### สถานะใน Modern Windows workspace

ใช้ semantic color ชุดเดียวกันทุกหน้าและทุก component โดยแยก text, background และ line เพื่อให้สีสถานะยังอ่านได้ทั้ง light/dark และไม่ใช้สีเป็นของตกแต่ง

| ความหมาย | text | background | line | ตัวอย่าง |
|---|---|---|---|---|
| `success` | `--wire-success` | `--wire-success-bg` | `--wire-success-line` | สำเร็จ, เชื่อมต่อแล้ว, จับคู่แล้ว |
| `warning` | `--wire-warning` | `--wire-warning-bg` | `--wire-warning-line` | รอแพ็ค, รอคิว, ต้องตรวจ, duplicate |
| `danger` | `--wire-danger` | `--wire-danger-bg` | `--wire-danger-line` | ผิดพลาด, ตกหล่น, บันทึกไม่สำเร็จ |
| `info` | `--wire-info` | `--wire-info-bg` | `--wire-info-line` | รับเข้า Drive, ข้ามวัน, ข้อมูลประกอบ |

การใช้งานหลักที่ต้องใช้ mapping เดียวกันคือ topbar/status bar, status banner, scan/camera feedback, queue, table badge, dashboard KPI/connection, Drive summary, Staff alert และ External Tools notice

ตาราง token กลุ่ม Modern/teal ด้านล่างยังคงไว้เพื่อ compatibility และประวัติการย้ายระบบ แต่ห้ามใช้เป็นค่าใหม่ใน `.wireframe-shell`

### รีโมทบนมือถือ (`/remote`) — ข้อยกเว้นเดียวของ desktop-only

หน้ารีโมทที่ `src/remote/` มีหน้าที่เดียว: เลือกขนส่งและคนแพ็คให้เครื่องที่สแกนอยู่ ไม่สแกนเอง ไม่มีรายงาน เป็น React tree ของตัวเองที่เลือกใน `src/main.jsx` ตั้งแต่ก่อน render จึงไม่มี `.enterprise-shell` และไม่มี `win-*` เลย

- ใช้ **token ชุดเดียวกับแอปหลัก** ที่ประกาศใน `src/styles.css` ทั้งหมด `src/remote/remote.css` ห้ามประกาศ `:root` ชุดที่สาม และห้าม hex/rgba หรือค่ามุมโค้งใหม่
- ตัวแปรที่มีเฉพาะรีโมท (`--remote-touch: 44px`) ประกาศใน scope `.remote-app` **ไม่ใช่ `:root`** — เทสต์ที่ตรวจว่าทุก `var()` resolve ได้ (ข้อ 10 ของหัวข้อ 6) กวาดเฉพาะ `:root` ของหน้าที่เปิดอยู่ ดังนั้น `RemoteApp` ต้องถูก `import()` แบบ dynamic เพื่อไม่ให้ CSS ของรีโมทติดเข้าไปในหน้าเดสก์ท็อป
- มีแท็บสองแท็บ **ห้องแพ็ค / ลง Drive** โดยห้องแพ็คเป็นค่าเริ่มต้น แต่ละแท็บคุมกระดานคนละใบ และแท็บ Drive ไม่มีปุ่มคนแพ็คเลยเพราะโหมดนั้นไม่บันทึกคนแพ็ค
- ปุ่มทุกตัวสูง **≥44px** (นิ้วโป้ง) ตรวจที่ 375px overflow แนวนอนต้องเป็น 0
- ปุ่มที่เลือกอยู่ใช้ `--primary-fill` เป็นพื้นคู่กับ `--on-primary` เสมอ ห้ามใช้ `--primary-fill` เป็นสีตัวหนังสือหรือสีขอบ (ดูตารางหัวข้อ 3)
- มี project Playwright ชื่อ `remote` แยกไว้ที่ viewport มือถือ **ห้ามเอา viewport มือถือกลับเข้า project `chromium`** เพราะชุด Desktop layout ตรวจ 1280/1440/1920

## 2. Token contract

ทั้งหมดประกาศที่ `:root` และ `:root[data-theme="dark"]` **ที่ต้นไฟล์ `src/styles.css` เท่านั้น**

### พื้นและตัวหนังสือ

| token | light | dark | ใช้ที่ |
|---|---|---|---|
| `--page` | `#F3F6F6` | `#04120f` | พื้นหลังพื้นที่ทำงาน |
| `--page-soft` | `#E8EDED` | `#0c211e` | พื้นรองที่ต้องจมลงไปอีกขั้น |
| `--surface` | `#ffffff` | `#14201f` | พื้นตาราง / sidebar / dialog |
| `--surface-soft` | `#F9FBFB` | `#0f1918` | แถบตัวกรอง, footer ของ dialog |
| `--text` | `#1c1c1e` | `#f5f5f7` | ตัวหนังสือหลัก |
| `--muted` | `#4A5654` | `#A6B1AF` | หัวคอลัมน์, label, แถบสถานะ |
| `--line` | `#E3E8E8` | `#223330` | เส้นแบ่งโครงสร้าง |
| `--line-strong` | `#CCD3D3` | `#2f4642` | เส้นใต้หัวตาราง, ขอบ dialog |

### พื้นรองและสถานะรายการ (ชื่อ token เดิมเพื่อ compatibility)

| token | light | dark | ใช้ที่ |
|---|---|---|---|
| `--win-chrome` | `#EDF1F1` | `#0f1918` | menu bar, toolbar, หัวตาราง, status bar |
| `--win-grid-line` | `#E9EDED` | `#1e2c2a` | เส้นตารางในกริด (บางกว่า `--line`) |
| `--win-row-alt` | `#FAFCFC` | `#111c1b` | แถวคู่ในตาราง |
| `--win-row-hover` | `#E9F5F4` | `#182726` | แถวที่เมาส์อยู่ |
| `--win-sel` | `#D6EEEB` | `#123c39` | พื้นแถวที่เลือก |
| `--win-sel-line` | `#1a6561` | `#3CC4BD` | เส้นซ้าย 3px ของแถวที่เลือก |

> `--win-sel` เทียบพื้นขาววัดได้เพียง **1.215** จึงบอกสถานะ "เลือกอยู่" ด้วยตัวเองไม่ได้ตามเกณฑ์ **เส้น `--win-sel-line` คือสิ่งที่ทำให้ผ่าน (6.823) ห้ามถอดออกแล้วเหลือแต่พื้น**

### แบรนด์ — เขียวฟ้า

| token | light | dark | ใช้เป็น |
|---|---|---|---|
| `--primary` | `#1e7671` | `#3CC4BD` | ตัวหนังสือ / ไอคอน / ขอบ |
| `--primary-strong` | `#1a6561` | `#3ae3db` | ขอบปุ่มหลัก, เส้นแถวที่เลือก, ตัวหนังสือบน tint |
| `--primary-fill` | `#2BA8A2` | `#2BA8A2` | **พื้น** ปุ่มหลักเท่านั้น |
| `--on-primary` | `#0a2320` | `#0a2320` | ตัวหนังสือบน `--primary-fill` |
| `--primary-soft` | `#E8F6F5` | `rgba(60,196,189,0.16)` | พื้นเมนูที่เลือกอยู่, hover ของปุ่ม |
| `--topbar-bg` | `#31363A` | `#151A1C` | compatibility token ของเปลือกเดิม; แถบบนใหม่ใช้ `--surface` |
| `--on-topbar` | `#ffffff` | `#ffffff` | ใช้คู่พื้นเข้มเดิมเท่านั้น; แถบบนใหม่ใช้ `--text` |

**`#2BA8A2` เป็นพื้นเท่านั้น ห้ามเป็นตัวหนังสือและห้ามเป็นขอบ** — บนขาววัดได้ 2.906 ตกทั้งเกณฑ์ตัวหนังสือ (4.5) และเกณฑ์ขอบ control (3.0) ตัวหนังสือบนพื้นนี้ต้องเป็น `--on-primary` ซึ่งเป็นสีเข้ม ไม่ใช่ขาว (ขาวบน `#2BA8A2` ได้ 2.906 เท่ากัน)

### ขอบของ control

| token | light | dark | ใช้ที่ |
|---|---|---|---|
| `--control-line` | `#788784` | `#637d78` | ขอบของ input / select / ปุ่ม / ช่องค้นหา |
| `--topbar-control-line` | `rgba(255,255,255,0.55)` | เท่ากัน | ขอบของ control ที่นั่งบน topbar |

`--line-strong` ใช้เป็นขอบ control **ไม่ได้** (วัดได้ 1.519 บนขาว) เป็นเส้นตกแต่งเท่านั้น

### สถานะ

| token | light | dark | ความหมาย |
|---|---|---|---|
| `--success` | `#176D35` | `#45b96c` | สำเร็จ / แพ็กแล้ว; ปรับจากค่าเดิมที่วัดได้ 4.342:1 บน tint เหนือพื้นหน้า |
| `--warning` | `#9c5205` | `#e8942f` | รอดำเนินการ / ต้องระวัง |
| `--danger` | `#b02b23` | `#ef6a60` | ล้มเหลว / ข้อมูลผิด |
| `--danger-fill` | `#b02b23` | `#bd544c` | พื้นป้ายนับงานค้าง (มี `--on-danger` ขาวทับ) |
| `--info` | `#14567f` | `#7cc0ea` | แจ้งให้ทราบ / รับเข้า Drive |

แต่ละตัวมี `--*-bg` เป็น tint 10–16% และ `--*-line` เป็นขอบ ใช้คู่กันเป็นป้ายสถานะในตาราง

### สเกลตัวอักษร

ประกาศเป็น **px** ไม่ใช่เศษของ `rem` — ค่าที่คำนวณจากฐาน 16px เคยทำให้ป้ายกำกับหล่นไปถึง
10.2px ซึ่งพยัญชนะไทยที่มีทั้งสระบนและสระล่างอ่านไม่ออกบนจอออฟฟิศ **12px คือพื้นล่างเด็ดขาด
ของทุกข้อความในแอป** มีเทสต์ e2e กวาดทุก element ที่มีตัวหนังสือของตัวเองบังคับข้อนี้อยู่

| token | ค่า | ใช้ที่ |
|---|---|---|
| `--ui-xs` | `12px` | ป้ายกำกับ, หัวคอลัมน์, แถบสถานะ, ตัวนับแถว — ห้ามต่ำกว่านี้ |
| `--ui-sm` | `14px` | ตัวหนังสือทั่วไป: เมนู ปุ่ม รายการ ช่องข้อมูลในตาราง |
| `--ui-md` | `16px` | หัวพาเนล, ชื่อโปรแกรม, label ของช่องสแกน |
| `--ui-lg` | `20px` | `h2` |
| `--ui-table` | `13px` | เนื้อหาและหัวตารางข้อมูล |
| `--ui-num` | `20px` | ตัวเลขสรุปที่ต้องอ่านจากระยะยืน |
| — | `19px` | ช่องยิงบาร์โค้ด (ใหญ่ที่สุดในหน้า เพราะเป็นเป้าหลักของงานทั้งวัน) |

น้ำหนักฟอนต์: **400** เป็นพื้น · **500** สำหรับ control และป้ายกำกับ · **600** สำหรับหัวข้อและตัวเลข
ระบบเดิมใช้ 700–900 แทบทุกที่ ซึ่งบนจอที่เปิดค้างทั้งวันอ่านยากกว่า ไม่ใช่ง่ายกว่า

### รูปทรง เงา จังหวะ

| token | ค่า | หมายเหตุ |
|---|---|---|
| `--radius-sm` | `8px` | ปุ่ม, ป้าย, ช่องกรอก |
| `--radius-md` | `12px` | dialog, พาเนล |
| `--shadow-modal` | `0 12px 32px rgba(16,24,32,0.18)` | dialog และ dropdown ของเมนู **เท่านั้น** |
| `--dur-press` | `0.12s` | press feedback |
| `--dur-ui` | `0.18s` | เพดานของ transition ทุกตัว |

ห้ามใช้ `--radius-lg/xl/full`, `--glow-*`, `--shadow-card`, `--ease-bounce` ในโค้ดใหม่ แม้ยังมี compatibility tokens ของเก่าอยู่

| token โครงหน้าจอ | ค่า |
|---|---|
| `--appbar-height` | `56px` |
| `--sidebar-width` | `224px` |
| `--sidebar-collapsed-width` | `64px` |
| `--control-height` | `36px` |
| `--space-layout` | `16px` |

---

## 3. ตาราง contrast ที่วัดแล้ว

วัดจาก DOM ของแอปจริง (`http://127.0.0.1:5173`) ครบทั้ง 4 หน้า × 2 ธีม ด้วยการกวาดทุก element ที่มีตัวหนังสือของตัวเอง composite สี alpha กับพื้นจริง และคิดเกณฑ์ตามขนาด/น้ำหนักฟอนต์รายตัว

**ตารางด้านล่างเป็นค่าที่บันทึกจากดีไซน์ก่อนหน้า ซึ่งใช้สีส่วนใหญ่ชุดเดียวกัน ไม่ใช่หลักฐานว่า layout ใหม่ผ่านแล้ว** สี success มีการแก้พร้อมหมายเหตุที่ token ต้องตรวจ Modern Workspace จาก DOM ใหม่ทุกหน้าและทุกธีมก่อนส่งงาน

> **ต้องหยุด transition ก่อนวัดเสมอ** — ใส่ `*{transition:none !important;animation:none !important}` ชั่วคราวก่อนอ่านค่า
> ไม่งั้นจะได้ค่าที่ค้างกลางทางของการสลับธีม เคยทำให้รายงานผิดว่ามีตัวหนังสือตกเกณฑ์ 11 รายการ
> ทั้งที่สีจริงถูกต้องหมด (หน้าที่ไม่ได้ถูก composite เพราะแพเนลถูกซ่อน จะไม่เดิน transition ต่อ)

### คู่ที่ต้องรู้ (เกณฑ์ 4.5:1)

| คู่สี | light | dark |
|---|---|---|
| `--text` / `--surface` | 17.015 | 15.354 |
| `--text` / `--win-chrome` | 14.951 | 16.448 |
| `--text` / `--win-row-alt` | 16.524 | 15.995 |
| `--text` / `--win-sel` | 14.008 | 11.141 |
| `--muted` / `--win-chrome` | 6.710 | 8.127 |
| `--muted` / `--surface` | 7.636 | 7.586 |
| `--primary` / `--surface` | 5.405 | 7.820 |
| `--primary` / `--win-chrome` | 4.750 | 8.377 |
| `--on-primary` / `--primary-fill` | 5.671 | 5.671 |
| `--on-topbar` / `--topbar-bg` | 12.214 | 17.552 |
| `--success` / `--surface` | 5.388 | 6.694 |
| `--warning` / `--surface` | 5.797 | 6.933 |
| `--danger` / `--surface` | 6.530 | 5.494 |
| `--info` / `--surface` | 7.873 | 8.427 |

`--muted` เดิมเป็น `#5c6b69` ซึ่งบน `--win-chrome` ได้ **4.905** คือผ่านแบบเฉียดฉิว และมันคือสีของ *ตัวหนังสือขนาดเล็กที่สุด* ในแอป (ป้ายกำกับ หัวคอลัมน์ แถบสถานะ) จึงเลื่อนลงมาเป็น `#4A5654` ได้ 6.710–7.636 บนพื้นทุกแบบ **ห้ามทำ `--muted` ให้จางกลับโดยไม่วัดใหม่**

### ขอบ control (เกณฑ์ 3:1)

| คู่สี | light | dark |
|---|---|---|
| `--control-line` / `--surface` | 3.752 | 3.770 |
| `--control-line` / `--win-chrome` | 3.297 | 4.038 |
| `--primary-strong` / `--win-chrome` (ขอบปุ่มหลัก) | 5.995 | 11.249 |
| `--win-sel-line` / `--surface` | 6.823 | 7.820 |
| เส้นซ้ายของเมนูที่เลือกอยู่ / `--surface` | 6.823 | 10.501 |
| ป้ายนับในปุ่มขนส่งที่เลือก (ตัวหนังสือ/พื้นของป้าย) | 6.505 | 6.505 |

### ค่าที่วัดแล้ว **ไม่ผ่าน** จึงห้ามใช้

| การใช้งาน | ค่า | เกณฑ์ | ต้องใช้อะไรแทน |
|---|---|---|---|
| `--primary-fill` `#2BA8A2` เป็นตัวหนังสือบนขาว | 2.906 | 4.5 | `--primary` `#1e7671` |
| ขาวเป็นตัวหนังสือบน `--primary-fill` | 2.906 | 4.5 | `--on-primary` `#0a2320` |
| `--primary-fill` เป็น**ขอบ**ปุ่มบนพื้น chrome | 2.554 | 3.0 | `--primary-strong` (5.995) |
| `--win-sel` บอกสถานะ "เลือก" ด้วยพื้นอย่างเดียว | 1.215 | 3.0 | ต้องมีเส้น `--win-sel-line` |
| `--primary-soft` บอกสถานะ "เมนูที่เปิดอยู่" ด้วยพื้นอย่างเดียว | 1.109 | 3.0 | ต้องมีเส้นซ้าย `--primary-strong` |
| `--primary-fill` เป็นเส้นบอกสถานะบนพื้น sidebar | 2.906 | 3.0 | `--primary-strong` |
| `--line-strong` เป็นขอบ control | 1.519 | 3.0 | `--control-line` |
| `--win-grid-line` เป็นขอบ control | 1.180 | 3.0 | `--control-line` (เส้นตารางเป็นของตกแต่ง ไม่อยู่ใต้เกณฑ์) |

---

## 4. Component

| ส่วน | ข้อกำหนด Modern Workspace |
|---|---|
| แถบบน | พื้น `--surface` ตัวหนังสือ `--text`; ชื่อระบบ บัญชี Sheet ธีม เสียง และเครื่องมือเพิ่มเติมอยู่แถบเดียว |
| เครื่องมือเพิ่มเติม | คงรีเฟรชและตรวจออเดอร์ที่หายจากทุกหน้า; การตรวจพาไป Admin ก่อน; คง auth/busy guards; ปิดเมื่อเลือกคำสั่ง, Escape, คลิกนอกหรือโฟกัสออก |
| เมนูซ้าย | ไอคอน+ชื่อเต็ม ลิงก์ภายนอกแยกกลุ่ม; เมนูยุบยังมี accessible name; ที่เลือกมีเส้นเข้มและพื้น tint |
| หัวหน้า/คำสั่ง | Packer/Admin มีหัวข้อ+รีเฟรช/นำเข้า, ค้นหาเฉพาะ Packer, ตรวจออเดอร์เฉพาะ Admin; ไม่มีแถบเมนู Windows หรือหัวข้อซ้ำ |
| พื้นที่สแกน | ลำดับเลือกขนส่ง/เลือก Packer/สแกน ผลลัพธ์ใกล้ช่องสแกน; ลิสต์ขนส่งเลื่อนภายใน; เครื่องมือรองพับได้ |
| Popup/QR | คงการเปิด การเลือก การคืนโฟกัสและ payload; QR source 512px และ layout preferences เดิม; ชื่อขึ้นบรรทัดได้ ไม่ตัดเป็นจุดไข่ปลา |
| รายงาน | หน้าเต็มไม่ใช่ disclosure; ตัวกรอง+คำสั่งอยู่แถบเดียว ห่อบรรทัดเมื่อจำเป็น; ยอดสรุปและคอลัมน์เดิม |
| ตาราง | ตัวอักษร 13px เส้นแนวนอน หัวติดบน เลื่อนเฉพาะกล่อง; คงสัญญาณสถานะและเลขพัสดุ monospace |
| พนักงาน/ตารางงาน | ใช้ typography/control/dialog ชุดเดียวกัน รักษาแผนผัง สิทธิ์ การจัดเวร และพื้นที่พิมพ์ |
| แถบสถานะ | 24px แสดงโหมด ยอดวันนี้ คิว และความล้มเหลว ไม่ซ่อนข้อมูลคิวเพื่อความสวยงาม |

---

## 5. เกณฑ์ที่ต้องผ่านก่อนถือว่างาน UI เสร็จ

1. **contrast วัดจาก DOM จริงทั้งสองธีม** ตัวหนังสือ 4.5:1 · ตัวใหญ่ (≥24px หรือ ≥18.66px bold) 3:1 · ขอบหรือพื้นที่ใช้ระบุตัว control 3:1 (WCAG 1.4.11) — กวาดทุก element ที่มีตัวหนังสือของตัวเอง ห้ามวัดตาม selector ที่เดาไว้ ห้ามประมาณ control ที่ `disabled` ได้รับการยกเว้น
2. **overflow แนวนอนเป็น 0px ที่ 1280 / 1440 / 1920** และไม่มีข้อความถูกตัด — ตารางที่กว้างเกินต้องเลื่อนอยู่ในกล่องของตัวเอง (`overflow-x: auto`) ไม่ใช่ดัน body
   *(เดิมเกณฑ์คือ 375 / 700 / 1000 / 1400 เปลี่ยนเพราะระบบนี้เป็น desktop-only แล้ว — ดูข้อ 1)*
3. **มี skip link** ไปเนื้อหาหลักเป็น focusable ตัวแรก ชี้ไป `<main id="main" tabindex="-1">`
4. **ทุก control เข้าถึงด้วยคีย์บอร์ดได้และมี focus ที่มองเห็น** ห้าม `outline: 0` โดยไม่มีตัวแทน
5. **ไม่มี transition ที่ไม่ได้ระบุ property** และไม่เกิน 0.2s (มีเทสต์ e2e บังคับอยู่)
6. **ไม่มีข้อความต่ำกว่า 12px** ที่ไหนเลยทั้งสองธีม (มีเทสต์ e2e บังคับอยู่)
7. **ทุก token ที่ถูกอ้างด้วย `var()` ต้อง resolve ได้จริง** (มีเทสต์ e2e บังคับอยู่ — ดูข้อ 10 ของหัวข้อ 6)

---

## 6. กับดักของไฟล์ `src/styles.css`

1. **`:root` ต้องมี 2 ชุดเท่านั้น** (light + dark) ตรวจด้วย `grep -c "^:root" src/styles.css` ต้องได้ **2** — เคยมีถึง 4 ชุด ชุดล่างชนะเงียบๆ ตาม source order ทำให้สีเพี้ยนซ้ำๆ หาสาเหตุไม่เจอ
2. **`.enterprise-shell` ครอบทั้งแอป** กฎที่ scope ด้วย class นี้ชนะกฎ unscoped เสมอ ถ้าแก้กฎ unscoped แล้วไม่เห็นผล ให้หากฎ `.enterprise-shell` ที่ทับอยู่
3. **ห้ามแก้อาการด้วย `!important` ทับกฎเดิม** ให้ลบกฎต้นทาง — เคยมีสองครึ่งของไฟล์สู้กันเอง (ครึ่งบน hardcode สีให้โหมด Packer/Drive ครึ่งล่างใช้ `!important` เกลี่ยกลับเป็นเทา) ผลคือการแยกสีที่ออกแบบไว้ไม่มีผลเลย
4. **ห้าม hardcode ค่ามุมโค้ง** เคยมีค่าดิบ 31 จุดที่ทำให้สเกลมุมโค้งที่ประกาศไว้ไม่มีผลจริง
5. **ห้าม `backdrop-filter: blur()` บนพาเนล** อนุญาตเฉพาะ overlay หลัง dialog
6. **ห้าม hover lift/scale ที่เป็นแค่การตกแต่ง** และพาเนลที่กดไม่ได้ต้องไม่ตอบสนอง hover เลย — อนุญาต `:active { scale(0.97) }` เป็น press feedback
7. **token ที่เลิกใช้แล้วยังอยู่ในไฟล์** (`--glow-*`, `--shadow-card`, `--accent-fill`, `--coral-fill`, `--info-fill`, `--radius-full`, `--ease-bounce`, `--field` แบบครีม) — ห้ามใช้ในโค้ดใหม่ ให้แทนที่ตอนที่แตะไฟล์นั้นอยู่แล้ว
8. **ความหนาแน่นของเนื้อหาถูกกำหนดในชั้น `.win-shell` ท้ายไฟล์** กฎเดิมของเนื้อหา (padding 12–22px, `min-height` 42–76px, `font-weight` 700–900) ยังอยู่ครบและถูกทับด้วย specificity ไม่ใช่ `!important` — ถ้าแก้ค่าเดิมแล้วไม่เห็นผล ให้หากฎ `.win-shell` ท้ายไฟล์ที่ทับอยู่
10. **comment ที่ไม่ปิดกลืนบรรทัดที่ตามมาแบบเงียบสนิท** — เคยเกิดสองครั้ง ครั้งหลังกลืน `--muted`
    `--line` และ `--line-strong` ออกจาก `:root` ผลคือ `border: 2px solid var(--line-strong)`
    กลายเป็นค่าที่ใช้ไม่ได้ทั้ง declaration ขอบของช่องยิงบาร์โค้ดจึงหายไปทั้งเส้นโดยไม่มี error
    และสคริปต์วัด contrast ก็ยังรายงาน 0 fail เพราะสีที่ตกทอดมาแทนบังเอิญยังผ่านเกณฑ์
    มีเทสต์ e2e กวาด `var(--x)` ทุกตัวในทุก stylesheet แล้วเช็คว่า resolve ได้จริง
9. **สคริปต์วัด contrast เข้าไม่ถึงหน้าจอที่ต้อง login** (popup สแกน, ตารางผลรายงาน, แผนผังพนักงาน) — เคยทำให้ปุ่มในหน้าต่างสแกนไม่เคยถูกวัดเลยจนเจอว่าขอบปุ่มแจ้งปัญหาได้ 1.237 และปุ่มปิดไม่มีขอบเลย (พื้นเทียบพื้นหน้าต่างได้ 1.039) วิธีตรวจคือฉีด markup ของหน้าจอนั้นเข้าไปในหน้าจริงแล้ววัด อย่าถือว่า "0 fail" จากสคริปต์แปลว่าทั้งแอปผ่าน

## 7. โมดูลแผนผังพนักงานและแผนงาน

- `STAFF_WORKSPACE_MODULE` ใน `src/features/staff/staffModules.js` เป็น registry ของโมดูลหลักและโมดูลย่อย ปุ่มนำทางและหน้าจอย่อยต้องอ่านจาก registry เพื่อให้เพิ่มโมดูลได้โดยไม่ยัดเงื่อนไขลงในหน้าแม่
- โมดูลหลักมีสี่รายการระดับเดียวกันใต้เมนูแผนผังพนักงาน: โครงสร้างทีม, ตารางเวร, แผนงานรายวัน และคลัง SOP; กระดานงานกับคลัง SOP เป็นคนละ component ใน `src/features/staff/modules/`
- SOP ระบุเจ้าของ วันที่เริ่มใช้ และกำหนดทบทวน; เวอร์ชันที่เผยแพร่เก็บ metadata ดังกล่าวแบบ immutable พร้อมขั้นตอน
- ตารางเวร (`staffWeeklyDuties` และงานเฉพาะวัน) เป็นแม่แบบ/ข้อมูลเดิม ห้ามนำมาแทนหรือย้ายทับแผนปฏิบัติงานรายวัน
- ฉบับ SOP ที่เผยแพร่ (`staffSopVersions`) เป็นข้อมูล immutable; แผนงาน (`staffWorkPlanTasks`) ต้องเก็บ snapshot ของเวอร์ชันที่ใช้จริง เพื่อให้การเผยแพร่รุ่นใหม่ไม่เปลี่ยนงานที่กำลังทำ
- งานแต่ละรายการมีลำดับและ prerequisite ที่เลือกได้; ค่าเริ่มต้นให้ต่อจากงานก่อนหน้า แต่ Admin ล้าง prerequisite เพื่อกำหนดงานขนานได้ ขั้นตอนบังคับใน SOP ต้องครบก่อนปิดงาน
- ประวัติการเปลี่ยนสถานะ/ขั้นตอน (`staffWorkPlanEvents`) เพิ่มได้อย่างเดียว พร้อมผู้บันทึกและเวลาจาก server; collection ใหม่ต้องคงกฎ read สำหรับเจ้าหน้าที่, write สำหรับ Admin และ query limit ที่มี guard เมื่อชนเพดาน
