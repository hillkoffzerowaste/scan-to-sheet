# AGENTS.md — คู่มือสำหรับ AI agent ในโปรเจกต์ scan-to-sheet

เอกสารนี้กำหนดวิธีทำงานของ AI agent ในโปรเจกต์นี้ อ่านให้จบก่อนแก้โค้ด

---

## 1. ภาษาที่ใช้สื่อสาร

**อธิบายทุกอย่างเป็นภาษาไทย** ทั้งระหว่างทำงานและตอนสรุป

- ก่อนลงมือแก้ไฟล์ ให้บอกเป็นภาษาไทยว่า **กำลังจะทำอะไร** และ **ทำไม**
- ระหว่างทำ ถ้าเจอสิ่งที่ไม่ตรงกับที่คิดไว้ ให้บอกทันทีเป็นภาษาไทย อย่าเงียบแล้วแก้ไปเรื่อยๆ
- ตอนจบ ให้สรุปเป็นภาษาไทยว่าแก้อะไรไปบ้าง ที่ไฟล์ไหน บรรทัดไหน และ**ยังเหลืออะไรที่ยังไม่ได้ทำ**
- ศัพท์เทคนิคที่ไม่มีคำไทยที่ใช้กันจริง ให้คงคำอังกฤษไว้ (เช่น commit, token, contrast, quota, batch, merge) ไม่ต้องแปลแบบฝืน
- **โค้ด คอมเมนต์ในโค้ด ชื่อตัวแปร และ commit message ยังเป็นภาษาอังกฤษตามเดิม** — ข้อกำหนดภาษาไทยนี้ใช้กับการ *สื่อสารกับผู้ใช้* เท่านั้น
- ข้อความที่ผู้ใช้ปลายทางเห็นในแอป (UI, error) ต้องเป็นภาษาไทย ดูข้อ 6

---

## 2. ขั้นตอนการทำงานเวลาเขียนโค้ด

ทำตามลำดับนี้ อย่าข้ามขั้น

### ขั้นที่ 1 — สำรวจก่อนแก้
- อ่านโค้ดจริงก่อนเสมอ **ห้ามเดาจากชื่อไฟล์หรือชื่อฟังก์ชัน**
- ค้นหาว่าโค้ดที่จะแก้ถูกเรียกจากที่ไหนบ้าง (`grep`) ก่อนเปลี่ยน signature หรือพฤติกรรม
- ถ้ามีเทสต์ครอบอยู่แล้ว อ่านเทสต์ด้วย เพราะบางครั้ง **เทสต์เขียนล็อกพฤติกรรมที่เป็นบั๊กไว้** (เคยเกิดขึ้นจริงในโปรเจกต์นี้)

### ขั้นที่ 2 — ยืนยันว่าเป็นบั๊กจริง
- อย่าเชื่อรายงานบั๊ก (รวมถึงของ agent อื่น) โดยไม่ตรวจเอง **ต้องพิสูจน์ด้วยการรันหรืออ่านโค้ดให้เห็นเส้นทางที่พัง**
- ถ้าเป็นเรื่องค่าที่วัดได้ (contrast, layout, ขนาด, จำนวน query) ให้ **วัดค่าจริง ห้ามประมาณ** ดูข้อ 5
- ถ้าตรวจแล้วไม่ใช่บั๊ก ให้บอกตรงๆ ว่าไม่ใช่ พร้อมเหตุผล อย่าแก้ทิ้งไว้เผื่อ

### ขั้นที่ 3 — แก้
- แก้ที่ **ต้นเหตุ** ไม่ใช่แก้ที่อาการ ตัวอย่าง: ถ้ามีกฎ CSS ที่ถูกกด `!important` ทับอยู่ ให้ลบกฎต้นทาง ไม่ใช่เพิ่ม `!important` ซ้อนอีกชั้น
- เขียนให้เข้ากับโค้ดรอบข้าง (naming, ความละเอียดของคอมเมนต์, สำนวนเดิมของไฟล์)
- ใส่คอมเมนต์อธิบาย **"ทำไม"** เฉพาะจุดที่คนอ่านทีหลังจะสงสัย โดยเฉพาะจุดที่แก้บั๊กแปลกๆ ให้ระบุค่าที่วัดได้ไว้ด้วย
- แก้ทีละเรื่อง อย่ารวมหลายเรื่องที่ไม่เกี่ยวกันใน commit เดียว

### ขั้นที่ 4 — ตรวจว่าไม่พัง
รันทั้งสองอย่างนี้ทุกครั้งก่อนบอกว่าเสร็จ

```bash
npm run test:marketplace
```

```bash
npm run build
```

- ถ้าเทสต์แดง **ห้ามบอกว่าเสร็จ** ต้องแก้ให้เขียวก่อน หรือรายงานตรงๆ ว่าแดงเพราะอะไร
- ถ้าแก้ UI ต้องเปิดดูของจริงด้วย ดูข้อ 5
- เพิ่มเทสต์ใหม่สำหรับบั๊กที่แก้ ถ้าเป็นฟังก์ชัน pure ที่เทสต์ได้

### ขั้นที่ 5 — รายงาน
- บอกเป็นภาษาไทยว่าแก้อะไร ที่ไหน และ**ผลการรันเทสต์ที่แท้จริง**
- **ระบุสิ่งที่ยังไม่ได้ตรวจให้ชัด** เช่น "หน้าที่ต้อง login ยังไม่ได้ตรวจ" — ห้ามพูดคลุมว่าเรียบร้อยทั้งหมด
- ถ้าเปลี่ยนเทสต์เดิม ต้องบอกด้วยว่าเปลี่ยนอันไหนและเพราะอะไร

---

## 3. คำสั่งที่ใช้บ่อย

| งาน | คำสั่ง |
|---|---|
| Unit test (ชุดหลัก ~79 เทสต์) | `npm run test:marketplace` |
| เทสต์ label-sync | `npm run test:label-sync` |
| E2E (Playwright) | `npm run test:e2e` |
| Build | `npm run build` |
| Marketplace sync worker | `npm run marketplace:sync:once` |

**ห้ามรัน dev server ด้วย Bash** ให้ใช้ preview tool ของ Claude Code (ตั้งค่าไว้แล้วที่ `.claude/launch.json` ชื่อ `scan-to-sheet` port 5173)

---

## 4. โครงสร้างที่ต้องรู้

```text
src/App.jsx                  state ทั้งหมดของแอป + ประกอบ shell เข้ากับ view (~3,300 บรรทัด)
src/shell/                   เปลือกแบบ Windows: TitleBar MenuBar Sidebar Toolbar StatusBar
src/views/                   หน้าจอ: WorkflowView (packer/drive) ReportsView ScanPopup StatusBanner
src/constants.js             ค่าคงที่ที่ทั้ง App และ view ใช้ร่วมกัน (ISSUE_*, CAMERA_*, PACKER_UNASSIGNED)
src/styles.css               CSS ทั้งหมด — มี :root สองชุด (light/dark) ห้ามสร้างซ้ำ (ดูข้อ 5)
src/services/
  googleSheets.js            เขียน/อ่าน Google Sheet, apiFetch, retry
  firebaseScans.js           Firestore: orders, marketplaceOrders, sync status
  marketplaceImport.js       parse ไฟล์ Shopee/Lazada/TikTok
  scanCommit.js              commit การสแกน
  sheetSyncReconciliation.js ตัดสินว่าสแกนซ้ำ/merge/สร้างแถวใหม่
api/                         Vercel serverless (OAuth, sheet lock)
scripts/marketplace-sync/    Playwright worker (ใช้ firebase-admin → ข้าม rules)
firestore.rules              ต้องแก้ตามเมื่อเพิ่ม field ใน Firestore
```

---

## 5. ระบบดีไซน์ (สำคัญ — เคยพังซ้ำหลายรอบ)

**ค่าสี ตัวเลข contrast ตาราง component และสเกลทั้งหมดอยู่ใน [DESIGN.md](DESIGN.md) ที่รากโปรเจกต์ — อ่านไฟล์นั้นก่อนแตะ CSS และแก้ค่าที่นั่นที่เดียว** หัวข้อนี้เก็บไว้เฉพาะกฎที่เคยพังจริงในโปรเจกต์นี้ ห้ามคัดลอกค่าสีหรือตัวเลขมาซ้ำที่นี่

ระบบคือ **Windows Enterprise Shell**: ระบบภายในบนคอมพิวเตอร์ในออฟฟิศ ไม่ใช่หน้าเว็บ — โครงเป็น title bar / menu bar / sidebar / toolbar / data grid / status bar, **desktop-only** (ออกแบบที่ 1280px ไม่มี breakpoint มือถือ), พื้นขาว-เทาไม่มีความอุ่น, เขียวฟ้า `#2BA8A2` เป็นสีเน้นสีเดียว, ขอบ 1px มุม 3–4px, ไม่มีเงายกเว้น dropdown กับ dialog

> **ระบบเดิมคือ *Flip7 บนโครง Hillkoff Enterprise* (ครีม `#FFF8E7`, ปุ่ม pill, glow เงาสี, จานสี gold/coral/sky) ถูกยกเลิกแล้ว** ห้ามใช้ในโค้ดใหม่ token ของระบบเดิมยังค้างอยู่ใน `src/styles.css` และยังถูกใช้อยู่หลายจุด ให้ทยอยแทนที่ตอนที่แตะไฟล์นั้นอยู่แล้ว ห้ามไล่แก้ทั้งไฟล์เป็น commit เดียว รายละเอียดสถานะการย้ายอยู่ที่หัวกล่องของ `DESIGN.md`

> หมายเหตุ: `DESIGN.md` ที่โฮมไดเรกทอรีของผู้ใช้ (`C:/Users/Office14/DESIGN.md`) เป็นค่าเริ่มต้นระดับองค์กร (Hillkoff / Website Technician) **ไม่ใช่มาตรฐานของโปรเจกต์นี้** — ตัวมันเองระบุในหัวข้อ 8 ว่าโปรเจกต์ที่มีระบบดีไซน์ของตัวเองสะสมมาแล้วให้ยึดของโปรเจกต์ ส่วนกฎวิศวกรรม 8 ข้อในไฟล์นั้นบังคับใช้กับโปรเจกต์นี้ด้วย

**กฎเหล็ก**

1. **สีต้องมาจาก CSS variable เท่านั้น ห้าม hardcode hex/rgba ใหม่** — token อยู่ใน `:root` และ `:root[data-theme="dark"]` ที่ **ต้นไฟล์ `src/styles.css` เท่านั้น** และห้าม hardcode ค่ามุมโค้งด้วย (เคยมีค่าดิบ 31 จุดที่ทำให้สเกลมุมโค้งที่ประกาศไว้ไม่มีผลจริง)
2. **ห้ามสร้าง `:root` ชุดที่สอง** — เคยมี `:root` ถึง 4 ชุดในไฟล์เดียว ชุดที่อยู่ล่างกว่าชนะเงียบๆ ตาม source order ทำให้สีเพี้ยนซ้ำๆ หาสาเหตุไม่เจอ ตรวจด้วย `grep -c "^:root" src/styles.css` ต้องได้ 2
3. **การ์ดต้องทึบ** ห้าม `backdrop-filter: blur()` บนการ์ด — อนุญาตเฉพาะ overlay หลัง modal (ปัจจุบัน 3px)
4. **ห้าม hover lift** (`translateY`) หรือ hover scale ที่เป็นแค่การตกแต่ง และ **พาเนลไม่ตอบสนอง hover เลย** เพราะกดไม่ได้ — อนุญาต `:active { scale(0.97) }` เป็น press feedback (แถวในตารางเป็นข้อยกเว้น: hover ได้เพราะคลิกเลือกได้จริง)
5. **transition ใส่เฉพาะ property ที่ต้องการ feedback** ไม่เกิน 0.2s ห้าม `transition: all` และห้าม blanket transition ครอบทุก element (เคยทำให้ UI หน่วงทั้งแอป) — มีเทสต์ e2e บังคับข้อนี้อยู่
6. **`.enterprise-shell` ครอบทั้งแอป** (`src/App.jsx`) — กฎที่ scope ด้วย class นี้จะชนะกฎ unscoped เสมอ ระวังเวลาแก้: ถ้าแก้กฎ unscoped แล้วไม่เห็นผล ให้หากฎ `.enterprise-shell` ที่ทับอยู่
7. **ห้ามแก้อาการด้วย `!important` ทับกฎเดิม** — เคยมีสองครึ่งของไฟล์สู้กันเอง (ครึ่งบน hardcode สีเขียว/ม่วงให้โหมด Packer/Drive ครึ่งล่างใช้ `!important` เกลี่ยกลับเป็นเทา) ผลคือการแยกสีที่ออกแบบไว้ไม่มีผลเลย ให้ลบกฎต้นทาง

**token ที่ต้องเข้าใจ** (ค่าอยู่ใน `DESIGN.md`)

- คู่ **fill / on-fill**: `--primary-fill` `#2BA8A2` เป็น *พื้น* เท่านั้น มีตัวหนังสือเข้ม `--on-primary` ทับ — เป็นตัวหนังสือไม่ได้ (2.906) เป็นขอบก็ไม่ได้ (2.554 บนพื้น chrome) ตัวขาวทับก็ไม่ได้ (2.906)
- `--primary` = เวอร์ชัน *ตัวหนังสือ/ขอบ* ของสีแบรนด์ · `--primary-strong` = ขอบปุ่มหลักและเส้นแถวที่เลือก
- กลุ่ม `--win-*` = เปลือกแบบ Windows (chrome, เส้นตาราง, แถวสลับ, แถว hover, แถวที่เลือก) **`--win-sel` บอกสถานะ "เลือกอยู่" ด้วยตัวเองไม่ได้ (1.215) ต้องมีเส้น `--win-sel-line` คู่เสมอ**
- `--on-danger` (ขาวบนแดง) และ `--on-topbar` (ขาวบนแถบเข้ม) แยกไว้ เพราะการฝัง `color: #fff` ตรงๆ คือสิ่งที่พังทันทีตอนพื้นเปลี่ยนสี
- `--control-line` = ขอบของ control ทุกชนิด ต้องได้ 3:1 (`--line-strong` ได้ 1.519 และ `--win-grid-line` ได้ 1.180 ใช้เป็นขอบ control ไม่ได้ทั้งคู่)
- token ของระบบเดิมที่ **เลิกใช้แล้ว**: `--glow-*`, `--shadow-card`, `--accent-fill`, `--coral-fill`, `--info-fill`, `--radius-lg/xl/full`, `--ease-bounce` และ `--field` แบบครีม

**เวลาแตะเรื่องสี ต้องวัด contrast จาก DOM จริง ห้ามเดา**

ใช้ preview tool เปิดแอป แล้วรัน script บนหน้าจริงทั้ง light และ dark โดย **กวาดทุก element ที่มีตัวหนังสือของตัวเอง** ไม่ใช่วัดตาม selector ที่เดาไว้ ต้อง composite สีที่มี alpha กับพื้นจริงก่อน และคิดเกณฑ์ตามขนาด/น้ำหนักฟอนต์ของแต่ละตัว (ปกติ **4.5:1** · ตัวใหญ่ ≥24px หรือ ≥18.66px bold **3:1**)

ต้องวัด **ขอบเขตของ control** ด้วย (WCAG 1.4.11 เกณฑ์ 3:1): ถ้า control มีขอบให้วัดสีขอบ ถ้าไม่มีให้วัดสีพื้นของมันเทียบพื้นรอบข้าง — ปุ่มที่ถมสีแบรนด์ไม่ผ่านด้วยตัวสีเอง (teal 2.554 บนพื้น chrome) จึงต้องมีขอบ `--primary-strong` (5.995) ส่วน control ที่ `disabled` ได้รับการยกเว้นตามข้อกำหนด

เหตุผลที่ต้องวัดจริง: เคยมีกรณีที่ขาวบน `--primary` วัดได้ **4.489** และ teal บนครีมได้ **4.465** คือตกเกณฑ์แบบมองด้วยตาไม่เห็น และทำให้ปุ่มหลักทุกปุ่มในแอปตกมาตรฐานพร้อมกัน ตอนย้ายมา Windows shell ก็เจออีกสี่จุดด้วยวิธีเดียวกัน: เส้นแถวที่เลือก 2.906 · ขอบป้ายจำนวนแถว 1.55 · เส้นซ้ายของเมนูที่เปิดอยู่ 2.906 · ป้ายนับในปุ่มขนส่งโหมด Drive ที่เป็นตัวเข้มบนพื้นเข้ม **1.031** (อ่านไม่ออกเลยแต่ดูผ่านตาไม่รู้สึก)

**เวลาวัดต้องหยุด transition ก่อน** ใส่ `*{transition:none !important;animation:none !important}` ชั่วคราว ไม่งั้นจะอ่านค่าที่ค้างกลางทางของการสลับธีม เคยรายงานผิดว่ามีตัวหนังสือตก 11 รายการทั้งที่สีถูกหมด

**ต้องตรวจ layout ที่หลายความกว้าง** 1280 / 1440 / 1920px — overflow แนวนอนต้องเป็น 0px และตารางที่กว้างเกินต้องเลื่อนในกล่องของตัวเอง ไม่ใช่ดัน body
*(เดิมคือ 375 / 700 / 1000 / 1400px เปลี่ยนเพราะระบบเป็น desktop-only แล้ว)*

> **ย้าย shell เสร็จแล้ว** เทสต์ที่เคยผูกกับมือถือถูกเปลี่ยนตามโครงใหม่: ชุด `Mobile Responsiveness` กลายเป็น `Desktop layout` ที่ตรวจ 1280/1440/1920 และ project `Pixel 5` ถูกถอดออกจาก `playwright.config.js`
>
> Capacitor/Android ยังอยู่ใน `package.json` ตั้งใจไม่ถอด การเลิกรองรับมือถือเป็นเรื่องของ *ดีไซน์* ไม่ใช่การถอด build target ถ้าจะถอดจริงต้องเป็นงานแยกที่ผู้ใช้สั่ง

---

## 6. ข้อความที่ผู้ใช้เห็น

- แอปนี้ใช้ **ภาษาไทย** ข้อความที่ผู้ใช้เห็นต้องเป็นไทยทั้งหมด รวมถึง `throw new Error(...)` ที่เด้งขึ้น status banner
- **ห้ามให้ข้อความ error หลุดรายละเอียดภายใน** เช่น response body ของ Google API (มี spreadsheet id/range) — เก็บไว้ที่ `error.detail` แล้วแสดงข้อความสรุปให้ผู้ใช้
- **ห้ามให้เทสต์ผูกกับข้อความที่แสดงผล** เพราะข้อความเปลี่ยนได้ตามการแปล ให้ใส่ `error.code` แล้วเทสต์เช็ค `code` (ตัวอย่าง: `GOOGLE_TIMEOUT`)
- ข้อความต้องตรงกับสิ่งที่โค้ดทำจริง — เคยมี title ว่าสำเร็จแต่เนื้อหาบอกว่าไม่มีอะไรเกิดขึ้น และมี guard ฝั่ง Admin ที่รายงานว่า "Packer row"
- คำที่ใช้ให้สม่ำเสมอ: `Packer` / `Admin` (ไม่ใช่ แพ็คเกอร์/แอดมิน), `เลขพัสดุ` ในประโยค, `Tracking` ในหัวคอลัมน์

---

## 7. กับดักเฉพาะโปรเจกต์นี้

**Google Sheet**

- คอลัมน์ C (Scan Date) **ต้องตรงกับชื่อชีตที่แถวนั้นอยู่** เพราะ `rowFromSheet` อ่าน `row.date` จากคอลัมน์นี้ และ `updateScanIssueGoogle` ใช้ค่านี้เลือกชีตที่จะไปค้น ถ้าไม่ตรงจะหาแถวไม่เจอ
- ค่าสถานะที่เก็บในชีต เช่น `'รอแพ็ค'` **ถูกใช้ในสูตร COUNTIF และ conditional format** เปลี่ยนคำสะกดไม่ได้โดยไม่ migrate ข้อมูล — แก้ได้แค่ข้อความชั้นแสดงผล
- การเขียนแถวทำสองจังหวะ (placeholder `_TEMP_...` แล้วค่อยเขียนเลขจริงทับ) ถ้าจังหวะสองล้มเหลว จะเหลือ `_TEMP_` ค้างในชีต
- lock การเขียนมี TTL จำกัด (ดู `api/sheet-lock.js`) การสแกนหนึ่งครั้งยิง API ~12 ครั้ง **ห้ามลบหรือแก้ตรวจสอบ verify แล้วสั่ง clear แถวทิ้งโดยไม่เช็คว่าแถวนั้นว่างจริง** เพราะอาจลบงานของเครื่องอื่น

**Firestore**

- **quota เป็นข้อจำกัดจริง** ทุก query/write ต้องมีขอบเขต ห้าม query ทั้ง collection โดยไม่ใส่ `limit()`
- การนำเข้า marketplace จำกัด **100 ออเดอร์ต่อครั้ง** และต้องนับให้ครอบคลุมทั้ง read และ write ไม่ใช่แค่จำนวน document ที่สร้างใหม่
- doc id ของ `marketplaceOrders` = `platform__orderId` แต่การจัดกลุ่มใช้ `platform__orderId__tracking` → ออเดอร์ที่มีหลายเลขพัสดุจะชนกัน ต้อง dedupe ก่อน batch
- `scripts/marketplace-sync/` ใช้ **firebase-admin ซึ่งข้าม security rules** เอกสารที่ worker สร้างอาจมี field ไม่ครบตามที่ `firestore.rules` บังคับ ทำให้ client เขียนทับไม่ผ่านและ batch ล้มทั้งชุด
- เพิ่ม field ใหม่ต้องแก้ `firestore.rules` ด้วย

**เปลือก Windows (`src/shell/` + `src/views/`)**

- **คำสั่งบน toolbar และ menu bar ต้องมีปลายทางอยู่จริงในแท็บนั้น** — พาเนล Lookup ถูกเรนเดอร์เฉพาะโหมด Packer ปุ่ม "ค้นหา" บนโหมด Drive จึงเคยกดแล้วเงียบสนิท เพราะ `focusSearch` หา `.search-panel` ไม่เจอ ปุ่มที่กดแล้วไม่เกิดอะไรคือบั๊กในสายตาผู้ใช้
- **เงื่อนไข `disabled` ของปุ่มบน toolbar ต้องตรงกับ guard ของฟังก์ชันที่มันเรียก** — เคยใช้ `isSignedIn` ขณะที่ `uploadMarketplaceFiles` เช็ค `firebaseUser` และไม่กันการอัปโหลดซ้อน ผลคือเลือกไฟล์ได้แต่ไม่มีอะไรเกิดขึ้นและไม่มีข้อความบอก
- **skip link ต้องชี้ที่ `<main>` ที่ไม่มี nav อยู่ข้างใน** — เคยชี้ที่ `div` ที่ครอบทั้ง menu bar และ sidebar กดแล้วกด Tab ต่อก็เข้าเมนูเหมือนเดิม คือไม่ได้ข้ามอะไรเลย (มีเทสต์ e2e ล็อกไว้แล้วในชุด `Shell regressions`)
- **`inputRef` ตัวเดียวถูกใช้ทั้งช่องสแกนในหน้าและช่องสแกนใน popup** ซึ่งอยู่ใน DOM พร้อมกันตอน popup เปิด ตอนปิด popup React จะเซ็ต ref เป็น `null` ทั้งที่ช่องในหน้ายังอยู่ → `focusScanInput` ไม่ทำงานจนกว่าจะ re-render ครั้งถัดไป ตอนนี้ถูกกลบด้วยนาฬิกาที่ `setToday` ทุก 1 วินาที ช่องโหว่จึงไม่เกิน 1 วิ **ยังไม่ได้แก้เพราะพิสูจน์ไม่ได้ถ้าไม่ login จริง** ถ้าจะแก้ต้องให้ ref ของ popup เคลียร์เฉพาะตอนที่ตัวเองเป็นเจ้าของ ref อยู่

**Build / JSX**

- **โปรเจกต์นี้ไม่มี `vite.config.js`** JSX จึงถูกแปลงด้วย esbuild แบบ classic ไม่ใช่ automatic runtime → **ทุกไฟล์ `.jsx` ต้อง `import React from 'react'` เอง** ไม่งั้นพังเป็น `ReferenceError: React is not defined` ตอน render **ไม่ใช่ตอน build** (`npm run build` ผ่านเฉยเลย) — เจอครั้งแรกตอนแยก `ReportsView` ออกจาก `App.jsx`
- ผลที่ตามมา: **`npm run build` ผ่าน ไม่ได้แปลว่า component ใหม่ render ได้** ต้องเปิดหน้าจริงด้วย preview tool แล้วกดเข้าไปในหน้านั้นเสมอ

**เวลา**

- แอปทำงานตามเวลา **Asia/Bangkok (UTC+7)** ค่าวันเวลาในชีตเป็นเวลาท้องถิ่น ถ้าจะแปลงเป็น instant ต้องหักออฟเซ็ต — เคยมีบั๊กที่คลาดไป 7 ชั่วโมงเพราะใช้ `Date.UTC` ตรงๆ

---

## 8. Git

- แยก commit ตามเรื่อง เขียน commit message เป็นภาษาอังกฤษ อธิบาย **อาการที่ผู้ใช้เจอ** และ **สาเหตุ** ไม่ใช่แค่บอกว่าแก้ไฟล์อะไร
- **push เมื่อผู้ใช้สั่งเท่านั้น**
- ก่อน `git add` ให้ดูว่ามีอะไรติดมาด้วย อย่าเผลอ commit ไฟล์ที่ผู้ใช้แก้ค้างไว้เอง หรือไฟล์ชั่วคราว (`.codex-tmp/`, `debug.log`, `playwright-report/`, `test-results/`)
- ลงท้าย commit message ด้วย:

```text
Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
```
